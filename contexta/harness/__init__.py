"""contexta/harness — Developer utilities for database seeding and inspection.

These scripts are intended for local development and CI bootstrapping only.
They are NOT imported by the main application at runtime.

Usage (from the repo root)::

    python -m contexta.harness.seed_database
    python -m contexta.harness.activate_blueprint
"""
